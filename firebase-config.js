/* ============================================================
   CONFIGURATION FIREBASE — E-School Finance
   ============================================================
   1. Allez sur https://console.firebase.google.com
   2. Créez un projet (gratuit).
   3. Dans le projet : « Créer une base de données » → Firestore Database
      → démarrer en mode TEST (ou voir firestore.rules.txt fourni).
   4. Paramètres du projet (icône ⚙️) → « Vos applications » → Ajouter une
      application Web (icône </>) → copiez les valeurs ci-dessous.
   5. Remplacez les valeurs "VOTRE_..." par les vôtres et enregistrez.
   ============================================================ */

window.firebaseConfig = {
  apiKey: "VOTRE_API_KEY",
  authDomain: "VOTRE_PROJET.firebaseapp.com",
  projectId: "VOTRE_PROJET_ID",
  storageBucket: "VOTRE_PROJET.appspot.com",
  messagingSenderId: "VOTRE_SENDER_ID",
  appId: "VOTRE_APP_ID"
};
